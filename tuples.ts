/// when arrays

let arr = ["shehroz", 23, true];
arr[0] = 24;
arr[1] = "kamran";
arr = [false, 37, "junaid"];

/// when tuples (when each position has been defined, it cann't be changed then)

let arr1: [string, number, boolean];
arr1 = ["Shehroz", 23, true];
// arr1[0] = 24;
// arr1[1] = "Alpha";
// arr1 = ["Shehroz", 23, true, "Accepted for proposal"];

// arr1=['shehroz',false,23]
